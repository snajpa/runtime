# Prefer the Nix flake (flake.nix); fall back to mise without Nix.
if command -v nix >/dev/null 2>&1 && [ -f flake.nix ]; then
  use flake
elif command -v mise &>/dev/null; then
  use_mise() {
    direnv_load mise direnv exec
  }
  use mise
fi
